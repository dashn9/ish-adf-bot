
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ish-bot-ebs-pvc
  namespace: bots
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ish-bot-ebs-sc
  resources:
    requests:
      storage: 200Gi