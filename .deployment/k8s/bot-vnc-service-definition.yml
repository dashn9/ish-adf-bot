apiVersion: v1
kind: Service
metadata:
  name: bot-vnc-service
  # You can switch to curstom namespace when opportuned
  namespace: default
spec:
  type: NodePort
  selector:
    name: webbot
    type: bot
  ports:
  - protocol: TCP
    port: 5900
    targetPort: 5900
